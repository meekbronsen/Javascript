// Circle and Square both use the method computeOptimumLocation. Instead of rewriting the code, we can inherit from where we had already written  
// Shape where computeOptimumLocation is, is refered to as the Base class/ Super / Parent
// Circle and Square are Derived class/Sub/Child because they inherit from Shape
// the relationshipw between Shape and Circle. IS-A 