const reset = document.querySelector(".use")
const pause = document.querySelector(".stopwatch--pause");

reset.setAttribute("xlink:href","images/sprite.svg#refresh");

pause.setAttribute("class","stopwatch__button stopwatch--refresh");