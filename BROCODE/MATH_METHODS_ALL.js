let x = 3.142;

let roundup =   Math.ceil(x);              // round up higher
let roundOff =  Math.round(3.642);         // round off the usual way
let roundDown = Math.floor(2.891);         // round down lower
let q =         Math.sqrt(2);              // square root
let r =         Math.pow(2, 3);            // 2 raised to the power of 3
let s =         Math.abs(-1);              // convert negative numbers to positive
let getMin =    Math.min(1, 22, 64);
let getMax =    Math.max(1, 22, 64);
let getValueOfPi = Math.PI;
let getRandomNumber= Math.random();       // random floats
let decimalPlaces = x.toFixed(2);         // to 2 decimal places

console.log(roundOff);
